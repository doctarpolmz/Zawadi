[build]
  command = "cd apps/web && npm run build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
>>>>>>> c19e2d1 (feat: premium UI redesign and Netlify deployment setup)
